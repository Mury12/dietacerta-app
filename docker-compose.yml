# docker-compose.yml
version: "2"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: app-dietacerta
    ports:
      - "8002:8002"
    command: yarn start
